<?
//
// $Id$
//
function BeAuthorized
   (
   $db_link,
   $email_addr,
   $agency,
   $mode = ""
   )
   {
   $saved_error_level = error_reporting(E_ALL);
   //
   $found_email_addr = "";
   //
   $result = $db_link->query
      (
      "select email_addr as found_email_addr from " . $mode . "user "
      .  "   where email_addr='$email_addr'"
      .  "      and agency='$agency' "
      )
      or die("BeAuthorized query failed with error:  " . mysqli_error());
   //
   if (mysqli_num_rows($result)) extract(mysqli_fetch_array($result));
   //
   error_reporting($saved_error_level);
   return ($found_email_addr != "");
   }
?>